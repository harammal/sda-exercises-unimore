#include "divisione.h"

int main(void) {
	int q;
	
	q = Divisione(10, 4);
	q = Divisione(2, 1);
	q = Divisione(3, 1);
	q = Divisione(1, 1);
	q = Divisione(2, 2);

	q = Divisione(3, 7);
	q = Divisione(6, 2);
	q = Divisione(7, 2);
	q = Divisione(0, 2);
	q = Divisione(2, 0);

	return 0;
}