#include "divisione_negativi.h"

int main(void) {
    int q;

    q = DivisioneNegativi(2, 0);
    q = DivisioneNegativi(2, -1);
    q = DivisioneNegativi(-2, 1);
    q = DivisioneNegativi(-2, -1);

    q = DivisioneNegativi(3, 7);
    q = DivisioneNegativi(7, 3);
    q = DivisioneNegativi(12, 4);


    return 0;
}
